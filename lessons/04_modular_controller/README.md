# 4단계. 기능을 함수로 나눈 모듈형 코드

[전체 강의자료](../README.md) · [이전 단계: 센서별 기본 실습](../03_sensor_basics/README.md) · [다음 단계: 아두이노와 웹 연결](../05_web_serial/README.md)

## 권장 수업 시간과 결과물

- 권장 시간: 2차시
- 결과물: 센서 네 종류와 네오픽셀을 동시에 실행하는 Arduino 프로그램
- 사용 파일: [`04_modular_controller.ino`](../../arduino/04_modular_controller/04_modular_controller.ino)

3단계에서는 부품을 하나씩 따로 시험했습니다. 이번에는 그 기능을 한 파일에 합칩니다. 단순히 코드를 이어 붙이는 것이 아니라 입력, 판단, 출력, 확인의 역할을 함수로 나눕니다.

## 이번 단계에서 만들 것

3단계에서 따로 실행한 센서와 네오픽셀을 하나의 프로그램으로 합칩니다. 센서 읽기, 상태 결정, LED 출력, 시리얼 출력을 각각 함수로 나누어 기능을 찾고 수정하기 쉬운 구조로 만듭니다.

## 학습목표

- 여러 부품의 코드를 하나의 프로그램으로 통합할 수 있다.
- `setup()`, `loop()`, 기능 함수의 역할을 구분할 수 있다.
- `delay()`를 줄이고 `millis()`로 실행 주기를 관리하는 이유를 설명할 수 있다.
- 입력값, 결정값, 출력값을 구분해 프로그램 흐름을 설명할 수 있다.

## 핵심 질문

프로그램의 기능을 함수로 나누면 무엇이 쉬워질까?

## 완성 결과

- 가변저항을 돌리면 네오픽셀 밝기가 바뀝니다.
- 터치센서를 누를 때마다 네오픽셀 색이 바뀝니다.
- 조도와 초음파 거리는 계속 측정되어 시리얼 모니터에 표시됩니다.
- 조도와 거리의 자동 조명 기준은 실제 측정값을 확인한 뒤 추가합니다.

## 시작 전 확인

- [ ] 가변저항 A0, 조도센서 A5, 터치센서 D13을 확인했다.
- [ ] 초음파센서 TRIG D2, ECHO D3을 확인했다.
- [ ] 네오픽셀 데이터 핀 D7을 확인했다.
- [ ] 3단계에서 센서별 값의 변화를 확인했다.
- [ ] `Adafruit NeoPixel` 라이브러리를 설치했다.

한 센서라도 작동하지 않으면 통합 코드에서 원인을 찾기 더 어렵습니다. 먼저 3단계의 해당 센서 예제로 돌아가 부품 하나만 다시 확인합니다.

## 코드의 전체 흐름

`loop()`에는 다음 함수 호출만 순서대로 놓습니다.

```cpp
void loop() {
  readPotentiometer();
  readLightSensor();
  readTouchSensor();
  readDistanceSensor();
  updateColorFromTouch();
  decideLightOutput();
  updateNeoPixel();
  printSystemState();
}
```

| 구분 | 함수 | 역할 |
|---|---|---|
| 입력 | `readPotentiometer()` | 가변저항값 읽기 |
| 입력 | `readLightSensor()` | 조도값 읽기 |
| 입력 | `readTouchSensor()` | 흔들림을 제거한 터치 상태 읽기 |
| 입력 | `readDistanceSensor()` | 초음파 왕복 시간으로 거리 계산 |
| 결정 | `updateColorFromTouch()` | 터치 이벤트가 있으면 다음 색 선택 |
| 결정 | `decideLightOutput()` | 가변저항값으로 목표 밝기 결정 |
| 출력 | `updateNeoPixel()` | 결정된 색과 밝기를 실제 LED에 적용 |
| 확인 | `printSystemState()` | 입력·결정·출력 상태를 한 줄로 표시 |

`setup()`은 전원을 켰을 때 한 번 실행되고, `loop()`는 전원이 꺼질 때까지 반복됩니다. 직접 만든 함수는 복잡한 작업에 이름을 붙인 것입니다. `loop()`만 읽어도 프로그램이 어떤 순서로 움직이는지 알 수 있습니다.

## 1차시. 여러 입력을 동시에 읽기

### 1. Part 3 코드에서 계속 작성하기

Part 3에서 저장한 자신의 파일을 엽니다. `04_modular_controller.ino`는 아직 열지 않습니다.

1. 터치 입력을 안정화하는 상태 변수와 `initializeTouchState()`를 추가합니다.
2. `updateColorFromTouch()`를 작성하고 `loop()`에 호출합니다.
3. `decideLightOutput()`을 작성해 가변저항값을 목표 밝기로 바꿉니다.
4. `updateNeoPixel()`을 작성해 결정값을 실제 LED에 적용합니다.
5. `printSensorValues()`를 `printSystemState()`로 확장합니다.

각 함수는 작성 직후 호출하고, 그 기능만 바뀌는지 확인합니다.

약 0.2초마다 다음과 같은 한 줄이 출력됩니다.

```text
가변저항: 512 | 조도: 640 | 터치: 없음 | 거리(cm): 31.4 | 밝기: 40 | 색 번호: 0
```

### 2. 입력과 출력 구분하기

| 구분 | 이 프로젝트의 값 | 값이 만들어지는 곳 |
|---|---|---|
| 입력 | 가변저항, 조도, 터치, 거리 | 센서 읽기 함수 |
| 결정 | 목표 밝기, 색 번호 | 결정 함수 |
| 출력 | 네오픽셀 색과 밝기 | `updateNeoPixel()` |
| 확인 | 한글 상태 문장 | `printSystemState()` |

가변저항을 돌렸을 때 `가변저항`과 `밝기`가 함께 바뀌는지 확인합니다. 터치할 때는 `색 번호`가 한 단계만 증가해야 합니다.

## 왜 긴 `delay()`를 사용하지 않을까?

`delay(1000)`을 실행하면 아두이노는 1초 동안 다른 일을 하지 못합니다. 그러면 터치를 놓치거나 웹 통신이 끊기는 것처럼 보일 수 있습니다. 이 코드에서는 `millis()`로 시간이 지났는지만 확인해 여러 기능이 번갈아 빠르게 실행되도록 합니다.

초음파센서의 `delayMicroseconds()`는 신호 한 번을 만들기 위한 몇 마이크로초의 짧은 대기이므로 그대로 사용합니다.

### `millis()`로 주기 확인하기

```cpp
unsigned long now = millis();
if (now - lastPrintMs < PRINT_INTERVAL_MS) return;
lastPrintMs = now;
```

이 코드는 프로그램 전체를 멈추지 않고 출력할 시간이 되었는지만 확인합니다. 아직 200ms가 지나지 않았다면 출력 함수만 빠져나가고, 다른 센서 함수는 계속 실행됩니다.

## 2차시. 이벤트와 상태 이해하기

터치센서는 손을 대고 있는 동안 여러 번 `HIGH`를 읽을 수 있습니다. 그러나 원하는 동작은 한 번 접촉할 때 색이 한 단계만 바뀌는 것입니다.

```cpp
if (stableTouchDetected) touchPressedEvent = true;
```

- `stableTouchDetected`: 지금 손이 닿아 있는 상태
- `touchPressedEvent`: 새로 한 번 눌린 순간

상태와 사건을 구분하면 손을 계속 대고 있어도 색이 빠르게 반복 변경되지 않습니다. `TOUCH_DEBOUNCE_MS`는 짧은 신호 흔들림을 제거하는 시간입니다.

## 누적 실행 순서

1. 이전 단계 코드가 먼저 정상 작동하는지 확인합니다.
2. 이번 함수 하나를 직접 입력합니다.
3. `setup()` 또는 `loop()`에 호출문을 추가합니다.
4. 업로드하고 예상 결과와 실제 결과를 비교합니다.
5. 문제가 있으면 방금 추가한 함수와 호출문만 확인합니다.
6. 정상 작동하면 다음 함수를 추가합니다.

## 실습 A. 실행 주기 비교

`PRINT_INTERVAL_MS`를 바꾸어 같은 동작을 반복합니다.

| 출력 간격 | 10초 동안 예상 출력 수 | 실제 출력 수 | 값을 읽기 쉬운가? | 조작 반응을 보기 쉬운가? |
|---:|---:|---:|---|---|
| 100 ms | 약 100개 |  |  |  |
| 200 ms | 약 50개 |  |  |  |
| 1000 ms | 약 10개 |  |  |  |

측정값을 자주 보내면 변화는 자세히 보이지만 데이터가 많아집니다. 너무 느리게 보내면 짧은 변화를 놓칠 수 있습니다.

## 실습 B. 입력-결정-출력 추적

| 조작 | 입력값 변화 | 결정값 변화 | 실제 출력 변화 | 정상 여부 |
|---|---|---|---|---|
| 가변저항 최소→최대 |  |  |  |  |
| 터치 한 번 |  |  |  |  |
| 터치 길게 유지 |  |  |  |  |
| 조도센서 가리기 |  |  |  |  |
| 물체 30cm→100cm |  |  |  |  |

조도와 거리는 이번 단계에서 측정만 합니다. 값이 변해도 LED의 색과 밝기를 직접 결정하지 않는 것이 정상입니다.

## 학생이 수정할 부분

처음에는 코드 위쪽의 설정만 수정합니다.

| 설정 | 의미 | 처음 권장값 |
|---|---|---|
| `TOUCH_ACTIVE_HIGH` | 터치할 때 HIGH가 되는 센서인지 여부 | `true` |
| `MAX_LED_BRIGHTNESS` | USB 전원에서 사용할 최대 밝기 | `80` |
| `TOUCH_DEBOUNCE_MS` | 터치 신호 흔들림 제거 시간 | `50` |
| `DISTANCE_INTERVAL_MS` | 거리 측정 간격 | `100` |
| `PRINT_INTERVAL_MS` | 시리얼 출력 간격 | `200` |

`COLORS` 표의 RGB 값을 바꾸면 터치할 때 순환하는 색을 바꿀 수 있습니다.

## 정상 작동 확인표

- [ ] 시리얼 모니터에 네 종류 입력값이 한 줄로 표시된다.
- [ ] 가변저항을 돌리면 `밝기`와 실제 LED 밝기가 함께 변한다.
- [ ] 터치 한 번마다 `색 번호`가 한 단계만 증가한다.
- [ ] 손을 계속 대고 있어도 색이 빠르게 반복 변경되지 않는다.
- [ ] 조도와 거리값이 바뀌어도 다른 센서 입력이 멈추지 않는다.

## 오류를 찾는 순서

1. 시리얼 모니터에서 해당 입력값이 변하는지 확인합니다.
2. 입력값은 변하지만 LED가 바뀌지 않으면 `결정 함수`를 확인합니다.
3. 결정값은 변하지만 실제 LED가 바뀌지 않으면 `updateNeoPixel()`과 결선을 확인합니다.
4. 터치가 반대로 표시되면 `TOUCH_ACTIVE_HIGH`를 `false`로 바꿉니다.

이 순서는 문제를 입력, 결정, 출력 중 한 부분으로 좁혀 줍니다.

| 관찰 결과 | 문제가 있을 가능성이 큰 부분 |
|---|---|
| 입력값부터 변하지 않음 | 센서 결선, 핀 번호, 읽기 함수 |
| 입력은 변하지만 목표값이 그대로 | 결정 함수와 조건문 |
| 목표값은 변하지만 LED가 그대로 | 출력 함수, D7, 네오픽셀 전원 |
| 모든 동작은 되지만 상태 문장이 이상함 | 출력 형식과 변수 이름 |

## 아직 적용하지 않은 자동 기능

조도센서의 변화 방향과 실제 실내 범위, 초음파센서의 안정적인 접근 거리를 아직 측정하지 않았습니다. 따라서 이 단계에서는 두 값을 출력만 하고 조명 결정에는 사용하지 않습니다. 키트 확인 후 다음을 추가합니다.

- 조도가 어두울수록 조명을 밝게 보정하는 규칙
- 사람이 일정 거리 안에 있을 때만 자동 모드를 실행하는 규칙
- 경계값 부근에서 켜짐과 꺼짐이 반복되지 않도록 하는 여유 구간

## 도전과제

1. `COLORS` 배열에 원하는 RGB 색을 하나 추가합니다.
2. 시리얼 출력 주기를 바꾸고 센서 조작감이 달라지는지 비교합니다.
3. 함수 하나를 주석 처리해 그 기능만 멈추는지 확인한 뒤 다시 복원합니다.

## Part 4 마무리 질문

1. `setup()`과 `loop()`의 역할은 어떻게 다른가?
2. 긴 `delay()`가 터치와 웹 통신에 어떤 문제를 만들 수 있는가?
3. `stableTouchDetected`와 `touchPressedEvent`는 왜 따로 필요한가?
4. 센서값은 정상인데 LED가 바뀌지 않을 때 어떤 순서로 확인해야 하는가?

## 제출할 결과

- 정상 작동 확인표
- 실습 A의 실행 주기 비교표
- 실습 B의 입력-결정-출력 추적표
- Part 4 마무리 질문 답변

## GitHub 자료

활동과 자기 점검을 끝낸 뒤 확인합니다.

- [Part 4까지의 누적 모범답안](https://github.com/sunsam-yu/physical-ai-mood-light/blob/main/arduino/04_modular_controller/04_modular_controller.ino)
- [Part 4 비교·복구 안내](https://github.com/sunsam-yu/physical-ai-mood-light/tree/main/lessons/04_modular_controller)

자신의 파일을 바꾸지 말고 새 함수, 전역 상태 변수, `setup()`과 `loop()`의 호출 순서만 비교합니다.

## 다음 단계

5단계에서는 사람이 읽는 한글 상태 문장 대신 웹이 해석하기 쉬운 데이터 형식으로 센서값을 전송합니다. 브라우저의 연결 버튼을 눌러 Arduino Uno의 직렬 포트를 선택하고, 수신된 값을 웹 화면에 표시합니다.
